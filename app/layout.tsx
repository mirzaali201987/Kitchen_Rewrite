// source_handbook: week11-hackathon-preparation
// Root layout: loads Fraunces (serif display) + DM Sans (body),
// sets up the warm editorial cookbook color palette as CSS vars.

import "./globals.css";

export const metadata = {
  title: "Kitchen Rewrite — recipes, adapted",
  description: "Adapt any recipe to your dietary constraints, grounded in a substitution database with citations.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="" />
        <link
          href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,500;0,9..144,600;0,9..144,700;0,9..144,900;1,9..144,400;1,9..144,500&family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>{children}</body>
    </html>
  );
}
